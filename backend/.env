# Server
PORT=3000
# Database (SQLite)
NODE_ENV=development

# Redis
REDIS_URL=redis://localhost:6379

# Kafka
KAFKA_BROKER=localhost:9092

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server
PORT=3000

# Database Configuration (for future PostgreSQL use)
DB_USER=myapp_user
DB_PASSWORD=mypassword
DB_NAME=myapp_db
DB_HOST=localhost
DB_PORT=5432